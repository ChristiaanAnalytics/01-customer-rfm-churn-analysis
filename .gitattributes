# ================================
# Git LFS Configuration
# ================================

# General text settings
* text=auto

# ================================
# Core Data Files (Large Files)
# ================================
*.csv      filter=lfs diff=lfs merge=lfs -text
*.parquet  filter=lfs diff=lfs merge=lfs -text
*.zip      filter=lfs diff=lfs merge=lfs -text

# ================================
# Jupyter & Analysis Files
# ================================
*.ipynb    filter=lfs diff=lfs merge=lfs -text

# ================================
# Media Assets
# ================================
*.mp4      filter=lfs diff=lfs merge=lfs -text
*.mov      filter=lfs diff=lfs merge=lfs -text
*.wav      filter=lfs diff=lfs merge=lfs -text
*.png      filter=lfs diff=lfs merge=lfs -text
*.jpg      filter=lfs diff=lfs merge=lfs -text
*.jpeg     filter=lfs diff=lfs merge=lfs -text

# ================================
# Optional Large Binary Files
# (add as needed for your project)
# ================================
*.tar      filter=lfs diff=lfs merge=lfs -text
*.gz       filter=lfs diff=lfs merge=lfs -text
*.bz2      filter=lfs diff=lfs merge=lfs -text

# ================================
# Notes
# - Git LFS tracks large files so your repo doesn’t exceed the 100MB GitHub limit.
# - Add other patterns here if your project introduces new file types.
# ================================
